# Stack (Ridge meta) artifacts

Generated by the **export cell** in `model/experiments-pool/98c-stack-ridge-meta-logreturn-pool.ipynb` (run after training):

- **stack_ridge_meta_logreturn_artifact.joblib** — LGB, Ridge, scalers, meta-learner (no LSTM object; see `lstm_path`).
- **lstm_stack_ridge_meta.keras** — Keras LSTM base (saved separately; .keras for Keras 3.x).

Backend loader: `stack_ridge_meta.py` in this directory.
